#Test Application Properties# Embedded MongoDB für Tests (schneller und isoliert)
# spring.mongodb.embedded.version=4.0.21
# spring.data.mongodb.database=stellarcompass-test

# Oder lokale MongoDB
# spring.data.mongodb.uri=mongodb://localhost:27017/stellarcompass-test
#Datenbank Konfiguration
spring.data.mongodb.uri=${MONGODB_URI}
spring.data.mongodb.database=stellarcompass-test

#Security Configuration for Auth0 //platzhalter für später (dependencies auch in pom.xml hinzufügen bevor aktiviert wird)
#spring.security.oauth2.resourceserver.jwt.issuer-uri=https://${AUTH0_DOMAIN}/
